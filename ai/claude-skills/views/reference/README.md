Key reminders:
- Normalize machine names (no `_1` suffix) and add tags/admin descriptions.
- Configure per-display access, titles, and No Results messaging.
- Render via view modes; avoid unnecessary Ajax or blanket CSS classes.
