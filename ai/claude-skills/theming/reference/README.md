Key reminders:
- One-word machine names; avoid `theme` or base-theme prefixes.
- Prefer Twig templates and preprocess for classes; minimize overrides.
- SCSS split by responsibility, with semantic `twig-` and behavior `js-` class prefixes.
